{% extends "layout.njk" %}
{% block content %}
  <div class="chef">
    <div class="chef_img">
      <img src="./assets/chef.png" alt="Chef Img"/>
    </div>
    <div class="chef_text">
      <h2>As melhores receitas</h2>
      <p>
        Aprenda a construir os melhores pratos
        <br/>
        com receitas criadas por profissionais
        <br/>
        do mundo inteiro.
      </p>
    </div>
  </div>
  <div class="cards">
    {% for item in items %}
      <div class="card" id="{{ item.id}}">
        <img src="{{ item.image }}" alt="Imagem de {{item.title}}"/>
        <div class="text">
          <span id="text_01">{{ item.title }}</span>
          <span id="text_02">por
            {{item.author}}</span>
        </div>
      </div>
    {% endfor %}
  </div>
  <script src="/javascripts/script.js"></script>
{% endblock %}
